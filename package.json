{
  "name": "earth-logistics-inc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "build:static": "next build && rm -rf deploy && cp -R out deploy && cp -R api deploy/api && cp apache/.htaccess deploy/.htaccess && cp apache/UPLOAD-INSTRUCTIONS.md deploy/UPLOAD-INSTRUCTIONS.md && find deploy -name '__next.*.txt' -delete && find deploy -name '__next._*.txt' -delete && find deploy -name 'index.txt' -delete && echo '\\n✅ deploy/ ready — upload its contents (incl. /api and .htaccess) to your hosting public_html\\n'"
  },
  "dependencies": {
    "framer-motion": "^12.0.0",
    "lucide-react": "^0.577.0",
    "next": "^16.1.6",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-hook-form": "^7.71.0",
    "zod": "^4.0.0",
    "@hookform/resolvers": "^5.2.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.0",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "^16.1.6",
    "tailwindcss": "^4.0.0",
    "typescript": "^5"
  }
}
